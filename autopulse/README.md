# 🟦 README.md
- Dossier `autopulse/`

```markdown
# Module principal du moteur d’automatisme.

Contient :
- `state_machine.py` : logique d’état
- `sequence_engine.py` : moteur de séquences
- `io.py` : entrées/sorties virtuelles
- `api.py` : API FastAPI
- `sequences.yaml` : définition des séquences

Ce dossier constitue le cœur du système AutoPulse.
```
