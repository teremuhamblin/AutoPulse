# docker/
- Fichiers pour exécuter AutoPulse dans un conteneur.

```md
Contient :
- `Dockerfile` : image Python + FastAPI
- `compose.yaml` : lancement simple via Docker Compose
```

### Usage :
```bash
docker compose up --build
```
