# webui/

Interface web minimaliste pour AutoPulse Control.

```md
Contient :
- `index.html` : page principale
- `app.js` : rafraîchissement des données via l’API
```

La UI affiche :
- état de la machine
- entrées/sorties
- séquences en cours
